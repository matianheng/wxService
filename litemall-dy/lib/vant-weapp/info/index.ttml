<sjs src="../wxs/utils.sjs" module="utils" />

<view tt:if="{{ info !== null && info !== '' || dot }}" class="custom-class van-info {{ utils.bem('info', { dot }) }}" style="{{ customStyle }}">{{ dot ? '' : info }}</view>
